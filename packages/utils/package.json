{
  "name": "@anonym-messenger/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, side-effect-free helpers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet\" && exit 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anonym-messenger/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
