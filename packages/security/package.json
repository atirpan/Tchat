{
  "name": "@anonym-messenger/security",
  "version": "0.0.0",
  "private": true,
  "description": "Security primitives. SecureMemory, SafeStorage, Redaction, HKDF.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@anonym-messenger/types": "workspace:*",
    "@anonym-messenger/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
